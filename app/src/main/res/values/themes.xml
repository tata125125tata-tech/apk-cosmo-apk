<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Primary Cosmo Game Store Theme using Material 3 -->
    <style name="Theme.CosmoGames" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Remove System Action Bar & Title Bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Brand Colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Surface & Background -->
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/surface_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Backward compatibility alias -->
    <style name="Theme.MyApplication" parent="Theme.CosmoGames" />
</resources>
